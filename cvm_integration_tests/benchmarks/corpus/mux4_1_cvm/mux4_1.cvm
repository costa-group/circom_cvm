;; Prime value
%%prime 21888242871839275222246405745257275088548364400416034343698204186575808495617


;; Memory of signals
%%signals 86


;; Heap of components
%%components_heap 19


;; Types (for each field we store name type offset size nDims dims)


;; Main template
%%start Main_4


;; Component creation mode (implicit/explicit)
%%components implicit


;; Witness (signal list)
%%witness 0 1 2 36 37 38 39 61 62 63 64 65 66 67 68 69 70 71 72 73 74 75 77 78 79 80


%%template MultiMux4_0 [ ff 1 1] [ ff 2 1 16 ff 1 4] [41] [ ]
x_0 = i64.2
;; getting dest
ff.store i64.0 ff.1
;; end of store bucket
;;line 46
;; compute bucket
;; load bucket
x_1 = get_signal i64.18
;; end of load bucket
;; load bucket
x_2 = get_signal i64.17
;; end of load bucket
;; OP(MUL)
x_3 = ff.mul x_1 x_2
;; end of compute bucket
;; getting dest
set_signal i64.37 x_3
;; end of store bucket
;;line 48
;; compute bucket
;; load bucket
x_4 = get_signal i64.19
;; end of load bucket
;; load bucket
x_5 = get_signal i64.17
;; end of load bucket
;; OP(MUL)
x_6 = ff.mul x_4 x_5
;; end of compute bucket
;; getting dest
set_signal i64.38 x_6
;; end of store bucket
;;line 50
;; compute bucket
;; load bucket
x_7 = get_signal i64.19
;; end of load bucket
;; load bucket
x_8 = get_signal i64.18
;; end of load bucket
;; OP(MUL)
x_9 = ff.mul x_7 x_8
;; end of compute bucket
;; getting dest
set_signal i64.39 x_9
;; end of store bucket
;;line 52
;; compute bucket
;; load bucket
x_10 = get_signal i64.39
;; end of load bucket
;; load bucket
x_11 = get_signal i64.17
;; end of load bucket
;; OP(MUL)
x_12 = ff.mul x_10 x_11
;; end of compute bucket
;; getting dest
set_signal i64.40 x_12
;; end of store bucket
;;line 55
;; getting dest
ff.store i64.1 ff.0
;; end of store bucket
;; loop bucket. Line 55
loop
;; compute bucket
;; load bucket
x_13 = ff.load i64.1
;; end of load bucket
;; OP(LESSER)
x_14 = ff.lt x_13 ff.1
;; end of compute bucket
ff.if x_14
;;line 57
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_15 = get_signal i64.16
;; end of load bucket
;; load bucket
x_16 = get_signal i64.15
;; end of load bucket
;; OP(SUB)
x_17 = ff.sub x_15 x_16
;; end of compute bucket
;; load bucket
x_18 = get_signal i64.14
;; end of load bucket
;; OP(SUB)
x_19 = ff.sub x_17 x_18
;; end of compute bucket
;; load bucket
x_20 = get_signal i64.13
;; end of load bucket
;; OP(ADD)
x_21 = ff.add x_19 x_20
;; end of compute bucket
;; load bucket
x_22 = get_signal i64.12
;; end of load bucket
;; OP(SUB)
x_23 = ff.sub x_21 x_22
;; end of compute bucket
;; load bucket
x_24 = get_signal i64.11
;; end of load bucket
;; OP(ADD)
x_25 = ff.add x_23 x_24
;; end of compute bucket
;; load bucket
x_26 = get_signal i64.10
;; end of load bucket
;; OP(ADD)
x_27 = ff.add x_25 x_26
;; end of compute bucket
;; load bucket
x_28 = get_signal i64.9
;; end of load bucket
;; OP(SUB)
x_29 = ff.sub x_27 x_28
;; end of compute bucket
;; load bucket
;;line 58
x_30 = get_signal i64.8
;; end of load bucket
;; OP(SUB)
;;line 57
x_31 = ff.sub x_29 x_30
;; end of compute bucket
;; load bucket
;;line 58
x_32 = get_signal i64.7
;; end of load bucket
;; OP(ADD)
;;line 57
x_33 = ff.add x_31 x_32
;; end of compute bucket
;; load bucket
;;line 58
x_34 = get_signal i64.6
;; end of load bucket
;; OP(ADD)
;;line 57
x_35 = ff.add x_33 x_34
;; end of compute bucket
;; load bucket
;;line 58
x_36 = get_signal i64.5
;; end of load bucket
;; OP(SUB)
;;line 57
x_37 = ff.sub x_35 x_36
;; end of compute bucket
;; load bucket
;;line 58
x_38 = get_signal i64.4
;; end of load bucket
;; OP(ADD)
;;line 57
x_39 = ff.add x_37 x_38
;; end of compute bucket
;; load bucket
;;line 58
x_40 = get_signal i64.3
;; end of load bucket
;; OP(SUB)
;;line 57
x_41 = ff.sub x_39 x_40
;; end of compute bucket
;; load bucket
;;line 58
x_42 = get_signal i64.2
;; end of load bucket
;; OP(SUB)
;;line 57
x_43 = ff.sub x_41 x_42
;; end of compute bucket
;; load bucket
;;line 58
x_44 = get_signal i64.1
;; end of load bucket
;; OP(ADD)
;;line 57
x_45 = ff.add x_43 x_44
;; end of compute bucket
;; load bucket
;;line 58
x_46 = get_signal i64.40
;; end of load bucket
;; OP(MUL)
;;line 57
x_47 = ff.mul x_45 x_46
;; end of compute bucket
;; getting dest
set_signal i64.21 x_47
;; end of store bucket
;;line 59
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_48 = get_signal i64.15
;; end of load bucket
;; load bucket
x_49 = get_signal i64.13
;; end of load bucket
;; OP(SUB)
x_50 = ff.sub x_48 x_49
;; end of compute bucket
;; load bucket
x_51 = get_signal i64.11
;; end of load bucket
;; OP(SUB)
x_52 = ff.sub x_50 x_51
;; end of compute bucket
;; load bucket
x_53 = get_signal i64.9
;; end of load bucket
;; OP(ADD)
x_54 = ff.add x_52 x_53
;; end of compute bucket
;; load bucket
x_55 = get_signal i64.7
;; end of load bucket
;; OP(SUB)
x_56 = ff.sub x_54 x_55
;; end of compute bucket
;; load bucket
x_57 = get_signal i64.5
;; end of load bucket
;; OP(ADD)
x_58 = ff.add x_56 x_57
;; end of compute bucket
;; load bucket
x_59 = get_signal i64.3
;; end of load bucket
;; OP(ADD)
x_60 = ff.add x_58 x_59
;; end of compute bucket
;; load bucket
x_61 = get_signal i64.1
;; end of load bucket
;; OP(SUB)
x_62 = ff.sub x_60 x_61
;; end of compute bucket
;; load bucket
x_63 = get_signal i64.39
;; end of load bucket
;; OP(MUL)
x_64 = ff.mul x_62 x_63
;; end of compute bucket
;; getting dest
set_signal i64.22 x_64
;; end of store bucket
;;line 60
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_65 = get_signal i64.14
;; end of load bucket
;; load bucket
x_66 = get_signal i64.13
;; end of load bucket
;; OP(SUB)
x_67 = ff.sub x_65 x_66
;; end of compute bucket
;; load bucket
x_68 = get_signal i64.10
;; end of load bucket
;; OP(SUB)
x_69 = ff.sub x_67 x_68
;; end of compute bucket
;; load bucket
x_70 = get_signal i64.9
;; end of load bucket
;; OP(ADD)
x_71 = ff.add x_69 x_70
;; end of compute bucket
;; load bucket
x_72 = get_signal i64.6
;; end of load bucket
;; OP(SUB)
x_73 = ff.sub x_71 x_72
;; end of compute bucket
;; load bucket
x_74 = get_signal i64.5
;; end of load bucket
;; OP(ADD)
x_75 = ff.add x_73 x_74
;; end of compute bucket
;; load bucket
x_76 = get_signal i64.2
;; end of load bucket
;; OP(ADD)
x_77 = ff.add x_75 x_76
;; end of compute bucket
;; load bucket
x_78 = get_signal i64.1
;; end of load bucket
;; OP(SUB)
x_79 = ff.sub x_77 x_78
;; end of compute bucket
;; load bucket
x_80 = get_signal i64.38
;; end of load bucket
;; OP(MUL)
x_81 = ff.mul x_79 x_80
;; end of compute bucket
;; getting dest
set_signal i64.23 x_81
;; end of store bucket
;;line 61
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_82 = get_signal i64.12
;; end of load bucket
;; load bucket
x_83 = get_signal i64.11
;; end of load bucket
;; OP(SUB)
x_84 = ff.sub x_82 x_83
;; end of compute bucket
;; load bucket
x_85 = get_signal i64.10
;; end of load bucket
;; OP(SUB)
x_86 = ff.sub x_84 x_85
;; end of compute bucket
;; load bucket
x_87 = get_signal i64.9
;; end of load bucket
;; OP(ADD)
x_88 = ff.add x_86 x_87
;; end of compute bucket
;; load bucket
x_89 = get_signal i64.4
;; end of load bucket
;; OP(SUB)
x_90 = ff.sub x_88 x_89
;; end of compute bucket
;; load bucket
x_91 = get_signal i64.3
;; end of load bucket
;; OP(ADD)
x_92 = ff.add x_90 x_91
;; end of compute bucket
;; load bucket
x_93 = get_signal i64.2
;; end of load bucket
;; OP(ADD)
x_94 = ff.add x_92 x_93
;; end of compute bucket
;; load bucket
x_95 = get_signal i64.1
;; end of load bucket
;; OP(SUB)
x_96 = ff.sub x_94 x_95
;; end of compute bucket
;; load bucket
x_97 = get_signal i64.37
;; end of load bucket
;; OP(MUL)
x_98 = ff.mul x_96 x_97
;; end of compute bucket
;; getting dest
set_signal i64.24 x_98
;; end of store bucket
;;line 62
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_99 = get_signal i64.13
;; end of load bucket
;; load bucket
x_100 = get_signal i64.9
;; end of load bucket
;; OP(SUB)
x_101 = ff.sub x_99 x_100
;; end of compute bucket
;; load bucket
x_102 = get_signal i64.5
;; end of load bucket
;; OP(SUB)
x_103 = ff.sub x_101 x_102
;; end of compute bucket
;; load bucket
x_104 = get_signal i64.1
;; end of load bucket
;; OP(ADD)
x_105 = ff.add x_103 x_104
;; end of compute bucket
;; load bucket
x_106 = get_signal i64.19
;; end of load bucket
;; OP(MUL)
x_107 = ff.mul x_105 x_106
;; end of compute bucket
;; getting dest
set_signal i64.25 x_107
;; end of store bucket
;;line 63
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_108 = get_signal i64.11
;; end of load bucket
;; load bucket
x_109 = get_signal i64.9
;; end of load bucket
;; OP(SUB)
x_110 = ff.sub x_108 x_109
;; end of compute bucket
;; load bucket
x_111 = get_signal i64.3
;; end of load bucket
;; OP(SUB)
x_112 = ff.sub x_110 x_111
;; end of compute bucket
;; load bucket
x_113 = get_signal i64.1
;; end of load bucket
;; OP(ADD)
x_114 = ff.add x_112 x_113
;; end of compute bucket
;; load bucket
x_115 = get_signal i64.18
;; end of load bucket
;; OP(MUL)
x_116 = ff.mul x_114 x_115
;; end of compute bucket
;; getting dest
set_signal i64.26 x_116
;; end of store bucket
;;line 64
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_117 = get_signal i64.10
;; end of load bucket
;; load bucket
x_118 = get_signal i64.9
;; end of load bucket
;; OP(SUB)
x_119 = ff.sub x_117 x_118
;; end of compute bucket
;; load bucket
x_120 = get_signal i64.2
;; end of load bucket
;; OP(SUB)
x_121 = ff.sub x_119 x_120
;; end of compute bucket
;; load bucket
x_122 = get_signal i64.1
;; end of load bucket
;; OP(ADD)
x_123 = ff.add x_121 x_122
;; end of compute bucket
;; load bucket
x_124 = get_signal i64.17
;; end of load bucket
;; OP(MUL)
x_125 = ff.mul x_123 x_124
;; end of compute bucket
;; getting dest
set_signal i64.27 x_125
;; end of store bucket
;;line 65
;; compute bucket
;; load bucket
x_126 = get_signal i64.9
;; end of load bucket
;; load bucket
x_127 = get_signal i64.1
;; end of load bucket
;; OP(SUB)
x_128 = ff.sub x_126 x_127
;; end of compute bucket
;; getting dest
set_signal i64.28 x_128
;; end of store bucket
;;line 67
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_129 = get_signal i64.8
;; end of load bucket
;; load bucket
x_130 = get_signal i64.7
;; end of load bucket
;; OP(SUB)
x_131 = ff.sub x_129 x_130
;; end of compute bucket
;; load bucket
x_132 = get_signal i64.6
;; end of load bucket
;; OP(SUB)
x_133 = ff.sub x_131 x_132
;; end of compute bucket
;; load bucket
x_134 = get_signal i64.5
;; end of load bucket
;; OP(ADD)
x_135 = ff.add x_133 x_134
;; end of compute bucket
;; load bucket
x_136 = get_signal i64.4
;; end of load bucket
;; OP(SUB)
x_137 = ff.sub x_135 x_136
;; end of compute bucket
;; load bucket
x_138 = get_signal i64.3
;; end of load bucket
;; OP(ADD)
x_139 = ff.add x_137 x_138
;; end of compute bucket
;; load bucket
x_140 = get_signal i64.2
;; end of load bucket
;; OP(ADD)
x_141 = ff.add x_139 x_140
;; end of compute bucket
;; load bucket
x_142 = get_signal i64.1
;; end of load bucket
;; OP(SUB)
x_143 = ff.sub x_141 x_142
;; end of compute bucket
;; load bucket
x_144 = get_signal i64.40
;; end of load bucket
;; OP(MUL)
x_145 = ff.mul x_143 x_144
;; end of compute bucket
;; getting dest
set_signal i64.29 x_145
;; end of store bucket
;;line 68
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_146 = get_signal i64.7
;; end of load bucket
;; load bucket
x_147 = get_signal i64.5
;; end of load bucket
;; OP(SUB)
x_148 = ff.sub x_146 x_147
;; end of compute bucket
;; load bucket
x_149 = get_signal i64.3
;; end of load bucket
;; OP(SUB)
x_150 = ff.sub x_148 x_149
;; end of compute bucket
;; load bucket
x_151 = get_signal i64.1
;; end of load bucket
;; OP(ADD)
x_152 = ff.add x_150 x_151
;; end of compute bucket
;; load bucket
x_153 = get_signal i64.39
;; end of load bucket
;; OP(MUL)
x_154 = ff.mul x_152 x_153
;; end of compute bucket
;; getting dest
set_signal i64.30 x_154
;; end of store bucket
;;line 69
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_155 = get_signal i64.6
;; end of load bucket
;; load bucket
x_156 = get_signal i64.5
;; end of load bucket
;; OP(SUB)
x_157 = ff.sub x_155 x_156
;; end of compute bucket
;; load bucket
x_158 = get_signal i64.2
;; end of load bucket
;; OP(SUB)
x_159 = ff.sub x_157 x_158
;; end of compute bucket
;; load bucket
x_160 = get_signal i64.1
;; end of load bucket
;; OP(ADD)
x_161 = ff.add x_159 x_160
;; end of compute bucket
;; load bucket
x_162 = get_signal i64.38
;; end of load bucket
;; OP(MUL)
x_163 = ff.mul x_161 x_162
;; end of compute bucket
;; getting dest
set_signal i64.31 x_163
;; end of store bucket
;;line 70
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_164 = get_signal i64.4
;; end of load bucket
;; load bucket
x_165 = get_signal i64.3
;; end of load bucket
;; OP(SUB)
x_166 = ff.sub x_164 x_165
;; end of compute bucket
;; load bucket
x_167 = get_signal i64.2
;; end of load bucket
;; OP(SUB)
x_168 = ff.sub x_166 x_167
;; end of compute bucket
;; load bucket
x_169 = get_signal i64.1
;; end of load bucket
;; OP(ADD)
x_170 = ff.add x_168 x_169
;; end of compute bucket
;; load bucket
x_171 = get_signal i64.37
;; end of load bucket
;; OP(MUL)
x_172 = ff.mul x_170 x_171
;; end of compute bucket
;; getting dest
set_signal i64.32 x_172
;; end of store bucket
;;line 71
;; compute bucket
;; compute bucket
;; load bucket
x_173 = get_signal i64.5
;; end of load bucket
;; load bucket
x_174 = get_signal i64.1
;; end of load bucket
;; OP(SUB)
x_175 = ff.sub x_173 x_174
;; end of compute bucket
;; load bucket
x_176 = get_signal i64.19
;; end of load bucket
;; OP(MUL)
x_177 = ff.mul x_175 x_176
;; end of compute bucket
;; getting dest
set_signal i64.33 x_177
;; end of store bucket
;;line 72
;; compute bucket
;; compute bucket
;; load bucket
x_178 = get_signal i64.3
;; end of load bucket
;; load bucket
x_179 = get_signal i64.1
;; end of load bucket
;; OP(SUB)
x_180 = ff.sub x_178 x_179
;; end of compute bucket
;; load bucket
x_181 = get_signal i64.18
;; end of load bucket
;; OP(MUL)
x_182 = ff.mul x_180 x_181
;; end of compute bucket
;; getting dest
set_signal i64.34 x_182
;; end of store bucket
;;line 73
;; compute bucket
;; compute bucket
;; load bucket
x_183 = get_signal i64.2
;; end of load bucket
;; load bucket
x_184 = get_signal i64.1
;; end of load bucket
;; OP(SUB)
x_185 = ff.sub x_183 x_184
;; end of compute bucket
;; load bucket
x_186 = get_signal i64.17
;; end of load bucket
;; OP(MUL)
x_187 = ff.mul x_185 x_186
;; end of compute bucket
;; getting dest
set_signal i64.35 x_187
;; end of store bucket
;;line 74
;; load bucket
x_188 = get_signal i64.1
;; end of load bucket
;; getting dest
set_signal i64.36 x_188
;; end of store bucket
;;line 76
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_189 = get_signal i64.21
;; end of load bucket
;; load bucket
x_190 = get_signal i64.22
;; end of load bucket
;; OP(ADD)
x_191 = ff.add x_189 x_190
;; end of compute bucket
;; load bucket
x_192 = get_signal i64.23
;; end of load bucket
;; OP(ADD)
x_193 = ff.add x_191 x_192
;; end of compute bucket
;; load bucket
x_194 = get_signal i64.24
;; end of load bucket
;; OP(ADD)
x_195 = ff.add x_193 x_194
;; end of compute bucket
;; load bucket
x_196 = get_signal i64.25
;; end of load bucket
;; OP(ADD)
x_197 = ff.add x_195 x_196
;; end of compute bucket
;; load bucket
x_198 = get_signal i64.26
;; end of load bucket
;; OP(ADD)
x_199 = ff.add x_197 x_198
;; end of compute bucket
;; load bucket
x_200 = get_signal i64.27
;; end of load bucket
;; OP(ADD)
x_201 = ff.add x_199 x_200
;; end of compute bucket
;; load bucket
x_202 = get_signal i64.28
;; end of load bucket
;; OP(ADD)
x_203 = ff.add x_201 x_202
;; end of compute bucket
;; load bucket
x_204 = get_signal i64.20
;; end of load bucket
;; OP(MUL)
x_205 = ff.mul x_203 x_204
;; end of compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
;;line 77
x_206 = get_signal i64.29
;; end of load bucket
;; load bucket
x_207 = get_signal i64.30
;; end of load bucket
;; OP(ADD)
x_208 = ff.add x_206 x_207
;; end of compute bucket
;; load bucket
x_209 = get_signal i64.31
;; end of load bucket
;; OP(ADD)
x_210 = ff.add x_208 x_209
;; end of compute bucket
;; load bucket
x_211 = get_signal i64.32
;; end of load bucket
;; OP(ADD)
x_212 = ff.add x_210 x_211
;; end of compute bucket
;; load bucket
x_213 = get_signal i64.33
;; end of load bucket
;; OP(ADD)
x_214 = ff.add x_212 x_213
;; end of compute bucket
;; load bucket
x_215 = get_signal i64.34
;; end of load bucket
;; OP(ADD)
x_216 = ff.add x_214 x_215
;; end of compute bucket
;; load bucket
x_217 = get_signal i64.35
;; end of load bucket
;; OP(ADD)
x_218 = ff.add x_216 x_217
;; end of compute bucket
;; load bucket
x_219 = get_signal i64.36
;; end of load bucket
;; OP(ADD)
x_220 = ff.add x_218 x_219
;; end of compute bucket
;; OP(ADD)
;;line 76
x_221 = ff.add x_205 x_220
;; end of compute bucket
;; getting dest
set_signal i64.0 x_221
;; end of store bucket
;;line 55
;; getting dest
ff.store i64.1 ff.1
;; end of store bucket
continue
end
end
;; end of loop bucket

%%template Mux4_1 [ ff 0 ] [ ff 1 16 ff 1 4] [21] [ 0 ]
x_222 = i64.1
;;line 103
;; getting dest
ff.store i64.0 ff.0
;; end of store bucket
;;line 110
;; getting dest
ff.store i64.0 ff.0
;; end of store bucket
;; loop bucket. Line 110
loop
;; compute bucket
;; load bucket
x_223 = ff.load i64.0
;; end of load bucket
;; OP(LESSER)
x_224 = ff.lt x_223 ff.16
;; end of compute bucket
ff.if x_224
;;line 111
;; load bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_225 = ff.load i64.0
;; end of load bucket
;; OP(TO_ADDRESS)
x_226 = i64.wrap_ff x_225
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_227 = i64.mul i64.1 x_226
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_228 = i64.add x_227 i64.1
;; end of compute bucket
x_229 = get_signal x_228
;; end of load bucket
;; getting dest
;; compute bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
;;line 111
x_230 = ff.load i64.0
;; end of load bucket
;; OP(TO_ADDRESS)
x_231 = i64.wrap_ff x_230
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_232 = i64.mul i64.1 x_231
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_233 = i64.add i64.0 x_232
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_234 = i64.add x_233 i64.1
;; end of compute bucket
;;line 111
set_cmp_input_cnt_check i64.0 x_234 x_229
;; end of store bucket
;;line 110
;; compute bucket
;; load bucket
x_235 = ff.load i64.0
;; end of load bucket
;; OP(ADD)
x_236 = ff.add x_235 ff.1
;; end of compute bucket
;; getting dest
ff.store i64.0 x_236
;; end of store bucket
continue
end
end
;; end of loop bucket
;;line 114
;; getting dest
ff.store i64.0 ff.0
;; end of store bucket
;; loop bucket. Line 114
loop
;; compute bucket
;; load bucket
x_237 = ff.load i64.0
;; end of load bucket
;; OP(LESSER)
x_238 = ff.lt x_237 ff.4
;; end of compute bucket
ff.if x_238
;;line 115
;; load bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_239 = ff.load i64.0
;; end of load bucket
;; OP(TO_ADDRESS)
x_240 = i64.wrap_ff x_239
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_241 = i64.mul i64.1 x_240
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_242 = i64.add x_241 i64.17
;; end of compute bucket
x_243 = get_signal x_242
;; end of load bucket
;; getting dest
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
;;line 115
x_244 = ff.load i64.0
;; end of load bucket
;; OP(TO_ADDRESS)
x_245 = i64.wrap_ff x_244
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_246 = i64.mul i64.1 x_245
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_247 = i64.add x_246 i64.17
;; end of compute bucket
;;line 115
set_cmp_input_cnt_check i64.0 x_247 x_243
;; end of store bucket
;;line 114
;; compute bucket
;; load bucket
x_248 = ff.load i64.0
;; end of load bucket
;; OP(ADD)
x_249 = ff.add x_248 ff.1
;; end of compute bucket
;; getting dest
ff.store i64.0 x_249
;; end of store bucket
continue
end
end
;; end of loop bucket
;;line 118
;; load bucket
x_250 = get_cmp_signal i64.0 i64.0
;; end of load bucket
;; getting dest
set_signal i64.0 x_250
;; end of store bucket

%%template Num2Bits_2 [ ff 1 4] [ ff 0 ] [5] [ ]
x_251 = i64.4
;;line 0
;; getting dest
ff.store i64.0 ff.4
;; end of store bucket
;;line 28
;; getting dest
ff.store i64.1 ff.0
;; end of store bucket
;;line 30
;; getting dest
ff.store i64.2 ff.1
;; end of store bucket
;;line 31
;; getting dest
ff.store i64.3 ff.0
;; end of store bucket
;; loop bucket. Line 31
loop
;; compute bucket
;; load bucket
x_252 = ff.load i64.3
;; end of load bucket
;; OP(LESSER)
x_253 = ff.lt x_252 ff.4
;; end of compute bucket
ff.if x_253
;;line 32
;; compute bucket
;; compute bucket
;; load bucket
x_254 = get_signal i64.4
;; end of load bucket
;; load bucket
x_255 = ff.load i64.3
;; end of load bucket
;; OP(SHIFT_R)
x_256 = ff.shr x_254 x_255
;; end of compute bucket
;; OP(BITAND)
x_257 = ff.band x_256 ff.1
;; end of compute bucket
;; getting dest
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_258 = ff.load i64.3
;; end of load bucket
;; OP(TO_ADDRESS)
x_259 = i64.wrap_ff x_258
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_260 = i64.mul i64.1 x_259
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_261 = i64.add x_260 i64.0
;; end of compute bucket
;;line 32
set_signal x_261 x_257
;; end of store bucket
;; assert bucket
;; compute bucket
;; compute bucket
;; load bucket
;;line 33
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_262 = ff.load i64.3
;; end of load bucket
;; OP(TO_ADDRESS)
x_263 = i64.wrap_ff x_262
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_264 = i64.mul i64.1 x_263
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_265 = i64.add x_264 i64.0
;; end of compute bucket
x_266 = get_signal x_265
;; end of load bucket
;; compute bucket
;; load bucket
;;line 33
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_267 = ff.load i64.3
;; end of load bucket
;; OP(TO_ADDRESS)
x_268 = i64.wrap_ff x_267
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_269 = i64.mul i64.1 x_268
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_270 = i64.add x_269 i64.0
;; end of compute bucket
x_271 = get_signal x_270
;; end of load bucket
;; OP(SUB)
;;line 33
x_272 = ff.sub x_271 ff.1
;; end of compute bucket
;; OP(MUL)
x_273 = ff.mul x_266 x_272
;; end of compute bucket
;; OP(EQ(Single(1)))
x_274 = ff.eq x_273 ff.0
;; end of compute bucket
x_275 = ff.eqz x_274
ff.if x_275
error i64.0
end
;; end of assert bucket
;;line 34
;; compute bucket
;; load bucket
x_276 = ff.load i64.1
;; end of load bucket
;; compute bucket
;; load bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_277 = ff.load i64.3
;; end of load bucket
;; OP(TO_ADDRESS)
x_278 = i64.wrap_ff x_277
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_279 = i64.mul i64.1 x_278
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_280 = i64.add x_279 i64.0
;; end of compute bucket
x_281 = get_signal x_280
;; end of load bucket
;; load bucket
;;line 34
x_282 = ff.load i64.2
;; end of load bucket
;; OP(MUL)
x_283 = ff.mul x_281 x_282
;; end of compute bucket
;; OP(ADD)
x_284 = ff.add x_276 x_283
;; end of compute bucket
;; getting dest
ff.store i64.1 x_284
;; end of store bucket
;;line 35
;; compute bucket
;; load bucket
x_285 = ff.load i64.2
;; end of load bucket
;; load bucket
x_286 = ff.load i64.2
;; end of load bucket
;; OP(ADD)
x_287 = ff.add x_285 x_286
;; end of compute bucket
;; getting dest
ff.store i64.2 x_287
;; end of store bucket
;;line 31
;; compute bucket
;; load bucket
x_288 = ff.load i64.3
;; end of load bucket
;; OP(ADD)
x_289 = ff.add x_288 ff.1
;; end of compute bucket
;; getting dest
ff.store i64.3 x_289
;; end of store bucket
continue
end
end
;; end of loop bucket
;; assert bucket
;; compute bucket
;; load bucket
;;line 38
x_290 = ff.load i64.1
;; end of load bucket
;; load bucket
x_291 = get_signal i64.4
;; end of load bucket
;; OP(EQ(Single(1)))
x_292 = ff.eq x_290 x_291
;; end of compute bucket
x_293 = ff.eqz x_292
ff.if x_293
error i64.0
end
;; end of assert bucket

%%template Constants_3 [ ff 1 16] [] [16] [ ]
x_294 = i64.1
;;line 8
;; getting dest
ff.store i64.0 ff.0
;; end of store bucket
;;line 11
;; getting dest
set_signal i64.0 ff.123
;; end of store bucket
;;line 12
;; getting dest
set_signal i64.1 ff.456
;; end of store bucket
;;line 13
;; getting dest
set_signal i64.2 ff.789
;; end of store bucket
;;line 14
;; getting dest
set_signal i64.3 ff.12
;; end of store bucket
;;line 15
;; getting dest
set_signal i64.4 ff.111
;; end of store bucket
;;line 16
;; getting dest
set_signal i64.5 ff.222
;; end of store bucket
;;line 17
;; getting dest
set_signal i64.6 ff.333
;; end of store bucket
;;line 18
;; getting dest
set_signal i64.7 ff.4546
;; end of store bucket
;;line 19
;; getting dest
set_signal i64.8 ff.134523
;; end of store bucket
;;line 20
;; getting dest
set_signal i64.9 ff.44356
;; end of store bucket
;;line 21
;; getting dest
set_signal i64.10 ff.15623
;; end of store bucket
;;line 22
;; getting dest
set_signal i64.11 ff.4566
;; end of store bucket
;;line 23
;; getting dest
set_signal i64.12 ff.1223
;; end of store bucket
;;line 24
;; getting dest
set_signal i64.13 ff.4546
;; end of store bucket
;;line 25
;; getting dest
set_signal i64.14 ff.4256
;; end of store bucket
;;line 26
;; getting dest
set_signal i64.15 ff.4456
;; end of store bucket

%%template Main_4 [ ff 0 ] [ ff 0 ] [2] [ 3 1 2 ]
x_295 = i64.1
;;line 37
;; getting dest
ff.store i64.0 ff.0
;; end of store bucket
;;line 45
;; load bucket
x_296 = get_signal i64.1
;; end of load bucket
;; getting dest
set_cmp_input_run i64.2 i64.4 x_296
;; end of store bucket
;;line 46
;; getting dest
ff.store i64.0 ff.0
;; end of store bucket
;; loop bucket. Line 46
loop
;; compute bucket
;; load bucket
x_297 = ff.load i64.0
;; end of load bucket
;; OP(LESSER)
x_298 = ff.lt x_297 ff.4
;; end of compute bucket
ff.if x_298
;;line 47
;; load bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_299 = ff.load i64.0
;; end of load bucket
;; OP(TO_ADDRESS)
x_300 = i64.wrap_ff x_299
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_301 = i64.mul i64.1 x_300
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_302 = i64.add x_301 i64.0
;; end of compute bucket
x_303 = get_cmp_signal i64.2 x_302
;; end of load bucket
;; getting dest
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
;;line 47
x_304 = ff.load i64.0
;; end of load bucket
;; OP(TO_ADDRESS)
x_305 = i64.wrap_ff x_304
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_306 = i64.mul i64.1 x_305
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_307 = i64.add x_306 i64.17
;; end of compute bucket
;;line 47
set_cmp_input_cnt_check i64.1 x_307 x_303
;; end of store bucket
;;line 46
;; compute bucket
;; load bucket
x_308 = ff.load i64.0
;; end of load bucket
;; OP(ADD)
x_309 = ff.add x_308 ff.1
;; end of compute bucket
;; getting dest
ff.store i64.0 x_309
;; end of store bucket
continue
end
end
;; end of loop bucket
;;line 49
;; getting dest
ff.store i64.0 ff.0
;; end of store bucket
;; loop bucket. Line 49
loop
;; compute bucket
;; load bucket
x_310 = ff.load i64.0
;; end of load bucket
;; OP(LESSER)
x_311 = ff.lt x_310 ff.16
;; end of compute bucket
ff.if x_311
;;line 50
;; load bucket
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
x_312 = ff.load i64.0
;; end of load bucket
;; OP(TO_ADDRESS)
x_313 = i64.wrap_ff x_312
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_314 = i64.mul i64.1 x_313
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_315 = i64.add x_314 i64.0
;; end of compute bucket
x_316 = get_cmp_signal i64.0 x_315
;; end of load bucket
;; getting dest
;; compute bucket
;; compute bucket
;; compute bucket
;; load bucket
;;line 50
x_317 = ff.load i64.0
;; end of load bucket
;; OP(TO_ADDRESS)
x_318 = i64.wrap_ff x_317
;; end of compute bucket
;; OP(MUL_ADDRESS)
;;line 0
x_319 = i64.mul i64.1 x_318
;; end of compute bucket
;; OP(ADD_ADDRESS)
x_320 = i64.add x_319 i64.1
;; end of compute bucket
;;line 50
set_cmp_input_cnt_check i64.1 x_320 x_316
;; end of store bucket
;;line 49
;; compute bucket
;; load bucket
x_321 = ff.load i64.0
;; end of load bucket
;; OP(ADD)
x_322 = ff.add x_321 ff.1
;; end of compute bucket
;; getting dest
ff.store i64.0 x_322
;; end of store bucket
continue
end
end
;; end of loop bucket
;;line 53
;; load bucket
x_323 = get_cmp_signal i64.1 i64.0
;; end of load bucket
;; getting dest
set_signal i64.0 x_323
;; end of store bucket

